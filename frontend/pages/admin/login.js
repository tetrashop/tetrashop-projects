import { useState } from 'react';
import { useRouter } from 'next/router';
export default function Login() {
  const [u,setU]=useState(''); const [p,setP]=useState(''); const [err,setErr]=useState(''); const [load,setLoad]=useState(false); const router=useRouter();
  const submit=async(e)=>{e.preventDefault(); setErr(''); setLoad(true); try{const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p})}); const d=await res.json(); if(!res.ok)throw new Error(d.error||'خطا'); localStorage.setItem('token',d.token); router.push('/admin/dashboard');}catch(e){setErr(e.message);}finally{setLoad(false);}};
  return (<div style={{minHeight:'100vh',display:'flex',alignItems:'center',justifyContent:'center',background:'linear-gradient(135deg,#1e3a8a,#3b82f6)'}}><div style={{background:'white',borderRadius:20,padding:40,width:380,maxWidth:'90%'}}><h1 style={{textAlign:'center',color:'#059669'}}>🔐 ورود</h1>{err&&<p style={{color:'#dc2626',background:'#fee2e2',padding:10,borderRadius:8,textAlign:'center'}}>{err}</p>}<form onSubmit={submit} style={{display:'flex',flexDirection:'column',gap:15}}><input value={u} onChange={e=>setU(e.target.value)} placeholder="نام کاربری" required style={{padding:12,border:'2px solid #e5e7eb',borderRadius:12}} /><input value={p} onChange={e=>setP(e.target.value)} type="password" placeholder="رمز عبور" required style={{padding:12,border:'2px solid #e5e7eb',borderRadius:12}} /><button type="submit" disabled={load} style={{padding:12,background:load?'#9ca3af':'#059669',color:'white',border:'none',borderRadius:12,fontWeight:'bold'}}>{load?'در حال ورود...':'ورود'}</button></form><p style={{textAlign:'center',marginTop:15,color:'#6b7280',fontSize:14}}>admin / admin123</p></div></div>);
}
