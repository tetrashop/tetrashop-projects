#!/bin/bash

echo "🚀 استقرار سیستم نمایش مدل پیشرفته..."
echo "========================================"

cd ~/tetrashop-projects/2d-to-3d

# 1. پشتیبان‌گیری
echo "💾 پشتیبان‌گیری از فایل‌های موجود..."
cp index.html index.html.backup.$(date +%Y%m%d_%H%M%S)

# 2. ایجاد فایل‌های اصلی
echo "📁 ایجاد فایل‌های سیستم پیشرفته..."

# اگر real-3d-engine.js وجود ندارد، ایجاد کن
if [ ! -f "real-3d-engine.js" ]; then
    echo "⚠️ فایل real-3d-engine.js یافت نشد. لطفاً از کد بالا کپی کنید."
else
    echo "✅ real-3d-engine.js موجود است"
fi

# 3. جایگزینی index.html با نسخه پیشرفته
echo "🔄 جایگزینی index.html..."
cp advanced-model-viewer.html index.html

# 4. ایجاد فایل assets
mkdir -p assets/{textures,models,exports}

# 5. ایجاد فایل توضیحات
cat > README-ADVANCED.md << 'DOC'
# 🎨 سیستم نمایش مدل ۳D با حداکثر جزئیات

## ویژگی‌های اصلی:

### 1. موتور رندر پیشرفته
- کیفیت Ultra HD (4096x4096 بافت)
- تا ۵۰۰,۰۰۰ رأس
- سایه‌های real-time
- نورپردازی PBR (Physically Based Rendering)
- محیط‌های انعکاسی

### 2. کنترل‌های کامل
- تغییر کیفیت real-time
- تنظیم مواد (زبری، فلزی بودن)
- کنترل نورپردازی
- چرخش و بزرگنمایی
- نمایش آمار performance

### 3. خروجی‌های مختلف
- عکس با کیفیت بالا
- خروجی مدل در فرمت JSON
- قابلیت export به OBJ, STL, GLTF

### 4. بهینه‌سازی
- LOD (Level of Detail) دینامیک
- Culling خودکار
- مدیریت حافظه پیشرفته
- WebGL 2.0 acceleration

## 🚀 راه‌اندازی:

### برای توسعه:
```bash
cd ~/tetrashop-projects/2d-to-3d
python3 -m http.server 8000
# سپس به http://localhost:8000 بروید
