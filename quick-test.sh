#!/bin/bash

echo "🧪 تست سریع navigation..."

# تست localhost
python3 -m http.server 8000 &
SERVER_PID=$!

sleep 2

echo "🌐 سرور تست: http://localhost:8000"
echo "📱 تست دستی:"
echo "1. مرورگر را باز کنید"
echo "2. به آدرس بالا بروید"
echo "3. روی پروژه‌ها کلیک کنید"
echo "4. کنسول (F12) را بررسی کنید"

# تست اتوماتیک
curl -s http://localhost:8000 | grep -o "<title>[^<]*</title>"
echo "✅ سرور در حال اجرا است"

# منتظر بمانید
read -p "آیا تست را تمام کردید؟ (Enter برای ادامه) " 

# خاموش کردن سرور
kill $SERVER_PID
echo "✅ تست کامل شد"
